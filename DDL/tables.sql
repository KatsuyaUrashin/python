CREATE TABLE ABC (SHAIN_CD varchar2(10), SHIMEI varchar2(100), UMARE DATE)
/
CREATE TABLE ZOKU (SHAIN_CD varchar2(10), SSK_CD varchar2(8), KBN char(2))
/
ALTER TABLE ABC ADD(SEX char(1))
/
ALTER TABLE ABC ADD CONSTRAINT ABC_PK PRIMARY KEY (SHAIN_CD)
/
ALTER TABLE ZOKU ADD CONSTRAINT ZOKU_PK PRIMARY KEY (SHAIN_CD, SSK_CD)
/

CREATE TABLE OPERATE_LOG (
    LOG_ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    USE_TABLE VARCHAR2(50) NOT NULL,
    OPERATION VARCHAR2(100) NOT NULL,
    KEYS VARCHAR2(1000),
    RESULT VARCHAR2(100) NOT NULL,
    OPERATION_DATE DATE DEFAULT SYSDATE,
    OPERATION_USER VARCHAR2(50) NOT NULL,
    CONSTRAINT OPERATE_LOG_PK PRIMARY KEY (LOG_ID)
)
/
ALTER SESSION SET NLS_DATE_FORMAT = 'YYYY/MM/DD HH24:MI:SS';
ALTER DATABASE SET TIME_ZONE = 'Asia/Tokyo';
SELECT 
    DBTIMEZONE,
    SESSIONTIMEZONE,
    SYSDATE,
    SYSTIMESTAMP
FROM DUAL;

select sysdate from dual;
select systimestamp from dual;